{
  "scripts": {
    "build": "node generate-manifest.js",
    "deploy":"npx gh-pages -d . -b gh-pages",
     "deploy:taskflow": "npx gh-pages -d ./modules/taskFlow -b gh-pages --add --dest modules/taskflow"
 
  }
}

   